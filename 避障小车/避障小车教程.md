# 避障小车教程

# Arduino智能避障小车 - 详细制作教程

## 📋 项目概述

这是一个基于Arduino的智能避障小车，能自动检测前方障碍物并智能避开。小车使用超声波传感器探测距离，舵机转动传感器查看左右两侧，智能选择最佳避障路径。

------

## 🛠️ 所需材料清单

### 主要组件：

1. **Arduino UNO 开发板** ×1
2. **L293D/L298N电机驱动模块** ×1（或使用Adafruit Motor Shield）
3. **直流电机** ×4（带轮子）
4. **超声波传感器（HC-SR04）** ×1
5. **舵机（SG90）** ×1
6. **小车底盘套件** ×1（含电机支架）
7. **7.4V锂电池** ×1（或4节AA电池盒）
8. **杜邦线** 若干

### 可选工具：

- 螺丝刀套装
- 热熔胶枪
- 扎带
- 万用表

------

## 🔌 详细接线指南

### 步骤1：电机驱动模块接线

**如果使用L298N/L293D模块：**

text

```
左前电机  -> M1 (OUT1, OUT2)
右前电机  -> M2 (OUT3, OUT4)
左后电机  -> M3 (OUT1, OUT2) 
右后电机  -> M4 (OUT3, OUT4)
模块ENA/ENB -> 接5V（使能）
模块VCC     -> 接外部电源正极
模块GND     -> 接外部电源负极
模块逻辑电源 -> 接Arduino 5V
```



**如果使用Adafruit Motor Shield：**
直接将Shield插到Arduino UNO上，电机按顺序接在M1-M4端口。

### 步骤2：超声波传感器接线

text

```
HC-SR04 VCC  -> Arduino 5V
HC-SR04 GND  -> Arduino GND
HC-SR04 Trig -> Arduino A0
HC-SR04 Echo -> Arduino A1
```



### 步骤3：舵机接线

text

```
SG90 棕色线(GND) -> Arduino GND
SG90 红色线(VCC) -> Arduino 5V
SG90 橙色线(信号) -> Arduino 数字引脚10
```



### 步骤4：电源连接

text

```
锂电池正极 -> 电机驱动模块电源输入+
锂电池负极 -> 电机驱动模块电源输入-
电机驱动模块5V输出 -> Arduino Vin
电机驱动模块GND -> Arduino GND
```



⚠️ **注意：** 确保所有地线（GND）连接在一起！

------

## 💻 软件安装与设置

### 步骤1：安装Arduino IDE

1. 访问 Arduino官网 ([www.arduino.cc](https://www.arduino.cc/))
2. 下载对应操作系统的Arduino IDE
3. 安装并启动Arduino IDE

### 步骤2：安装所需库

打开Arduino IDE，按以下步骤安装库：

**方法一：通过库管理器安装**

1. 点击 **工具 → 管理库**
2. 搜索安装以下库：
   - **AFMotor**（控制电机）
   - **NewPing**（超声波传感器）
   - **Servo**（舵机控制）

**方法二：手动安装库**
如果库管理器找不到，可以手动安装：

1. 下载库文件（ZIP格式）
2. 点击 **项目 → 加载库 → 添加.ZIP库**

### 步骤3：上传代码

1. 复制提供的完整代码
2. 在Arduino IDE中粘贴
3. 选择正确的开发板：**工具 → 开发板 → Arduino UNO**
4. 选择正确的端口：**工具 → 端口 → 选择你的Arduino端口**
5. 点击上传按钮（→）



## 完整代码

```c++
// ARDUINO OBSTACLE AVOIDING CAR - 智能优化版
#include <AFMotor.h>  
#include <NewPing.h>
#include <Servo.h> 

// 引脚定义
#define TRIG_PIN A0 
#define ECHO_PIN A1 
#define MAX_DISTANCE 200 

// 速度参数
#define MAX_SPEED 190
#define START_SPEED 180
#define MIN_SPEED 150
#define TURN_SPEED 170  // 转向速度

// 避障参数
#define OBSTACLE_DISTANCE 20  // 障碍物检测距离（厘米）
#define SAFE_DISTANCE 30      // 安全距离
#define CRITICAL_DISTANCE 10  // 紧急停止距离

NewPing sonar(TRIG_PIN, ECHO_PIN, MAX_DISTANCE);

// 电机初始化
AF_DCMotor motor1(1, MOTOR12_8KHZ);
AF_DCMotor motor2(2, MOTOR12_8KHZ);
AF_DCMotor motor3(3, MOTOR34_1KHZ);
AF_DCMotor motor4(4, MOTOR34_1KHZ);

Servo myservo;

// 状态变量
boolean goesForward = false;
int distance = 100;
int speedSet = 0;
int avoidManeuver = 0;  // 避障策略状态

void setup() {
  // 初始化串口调试（可选）
  Serial.begin(9600);
  
  // 电源稳定等待
  delay(2000);
  
  // 舵机初始化
  myservo.attach(10);  
  myservo.write(90);  // 改为90度居中位置
  delay(1000);
  
  // 电机预测试
  testMotors();
  
  // 初始距离测量
  for(int i = 0; i < 3; i++) {
    distance = readPing();
    delay(100);
  }
  
  Serial.println("小车初始化完成！");
}

void testMotors() {
  Serial.println("电机测试中...");
  
  // 前转测试
  setAllMotors(FORWARD, 150);
  delay(200);
  setAllMotors(RELEASE, 0);
  delay(100);
  
  // 后转测试
  setAllMotors(BACKWARD, 150);
  delay(200);
  setAllMotors(RELEASE, 0);
  delay(200);
  
  Serial.println("电机测试完成");
}

void loop() {
  // 持续读取前方距离
  distance = readPing();
  
  Serial.print("前方距离: ");
  Serial.print(distance);
  Serial.print(" cm | 状态: ");
  
  // 紧急情况：太近了！
  if(distance <= CRITICAL_DISTANCE) {
    Serial.println("紧急停止！");
    emergencyStop();
    delay(200);
    moveBackward();
    delay(500);
    avoidObstacle();
  }
  // 一般障碍物
  else if(distance <= OBSTACLE_DISTANCE) {
    Serial.println("检测到障碍物，准备避障");
    moveStop();
    delay(100);
    avoidObstacle();
  }
  // 安全距离，正常前进
  else {
    Serial.println("正常前进");
    moveForward();
  }
  
  delay(50);  // 主循环延时
}

// 智能避障决策
void avoidObstacle() {
  moveStop();
  delay(200);
  
  // 先稍微后退
  moveBackward();
  delay(300);
  moveStop();
  delay(200);
  
  // 查看右侧距离
  int distanceR = lookRight();
  Serial.print("右侧距离: ");
  Serial.println(distanceR);
  
  // 查看左侧距离
  int distanceL = lookLeft();
  Serial.print("左侧距离: ");
  Serial.println(distanceL);
  
  delay(300);
  
  // 智能决策逻辑
  if(distanceR >= SAFE_DISTANCE && distanceL >= SAFE_DISTANCE) {
    // 两边都安全，选择距离更远的一侧
    if(distanceR > distanceL) {
      Serial.println("两边都安全，选择右侧（距离更远）");
      turnRight();
    } else {
      Serial.println("两边都安全，选择左侧（距离更远）");
      turnLeft();
    }
  }
  else if(distanceR >= SAFE_DISTANCE) {
    // 只有右侧安全
    Serial.println("右侧安全，向右转");
    turnRight();
  }
  else if(distanceL >= SAFE_DISTANCE) {
    // 只有左侧安全
    Serial.println("左侧安全，向左转");
    turnLeft();
  }
  else {
    // 两边都不安全，180度掉头
    Serial.println("两边都不安全，掉头");
    turnAround();
  }
  
  // 避障后继续前进
  delay(300);
  moveForward();
}

// 紧急停止
void emergencyStop() {
  setAllMotors(BACKWARD, MAX_SPEED);
  delay(100);
  moveStop();
}

// 掉头（180度）
void turnAround() {
  Serial.println("执行180度掉头");
  
  // 向右转180度（调整时间以达到180度）
  motor1.setSpeed(TURN_SPEED);
  motor2.setSpeed(TURN_SPEED);
  motor3.setSpeed(TURN_SPEED);
  motor4.setSpeed(TURN_SPEED);
  
  motor1.run(FORWARD);
  motor2.run(FORWARD);
  motor3.run(BACKWARD);
  motor4.run(BACKWARD);
  
  delay(1000);  // 调整这个时间以获得180度转弯
  
  moveStop();
  goesForward = true;
}

int lookRight() {
  myservo.write(30);  // 向右看（0-90度）
  delay(500);
  int distance = readPing();
  delay(100);
  myservo.write(90);  // 回到中间
  delay(200);
  return distance;
}

int lookLeft() {
  myservo.write(150);  // 向左看（90-180度）
  delay(500);
  int distance = readPing();
  delay(100);
  myservo.write(90);  // 回到中间
  delay(200);
  return distance;
}

int readPing() { 
  // 多次读取取平均值，提高准确性
  int total = 0;
  int validReadings = 0;
  
  for(int i = 0; i < 3; i++) {
    delay(40);
    int cm = sonar.ping_cm();
    
    // 过滤无效读数（0或太大）
    if(cm > 0 && cm < MAX_DISTANCE) {
      total += cm;
      validReadings++;
    }
  }
  
  if(validReadings > 0) {
    return total / validReadings;
  } else {
    return MAX_DISTANCE;  // 没有有效读数，返回最大距离
  }
}

// 设置所有电机
void setAllMotors(int dir, int speed) {
  motor1.setSpeed(speed);
  motor2.setSpeed(speed);
  motor3.setSpeed(speed);
  motor4.setSpeed(speed);
  
  motor1.run(dir);
  motor2.run(dir);
  motor3.run(dir);
  motor4.run(dir);
}

void moveStop() {
  setAllMotors(RELEASE, 0);
  goesForward = false;
}

void moveForward() {
  if(!goesForward) {
    goesForward = true;
    
    // 高扭矩启动
    setAllMotors(FORWARD, START_SPEED);
    delay(150);
    
    // 调整到运行速度
    for (speedSet = START_SPEED; speedSet >= MIN_SPEED; speedSet -= 5) {
      setAllMotors(FORWARD, speedSet);
      delay(10);
    }
  }
  
  // 保持前进
  setAllMotors(FORWARD, MIN_SPEED);
}

void moveBackward() {
  goesForward = false;
  
  // 后退
  setAllMotors(BACKWARD, START_SPEED);
  delay(200);
  
  // 调整后退速度
  setAllMotors(BACKWARD, MIN_SPEED);
}

void turnRight() {
  Serial.println("向右转");
  
  motor1.setSpeed(TURN_SPEED);
  motor2.setSpeed(TURN_SPEED);
  motor3.setSpeed(TURN_SPEED);
  motor4.setSpeed(TURN_SPEED);
  
  motor1.run(FORWARD);
  motor2.run(FORWARD);
  motor3.run(BACKWARD);
  motor4.run(BACKWARD);
  
  delay(400);  // 转弯时间，根据实际情况调整
  
  moveStop();
  goesForward = true;
}

void turnLeft() {
  Serial.println("向左转");
  
  motor1.setSpeed(TURN_SPEED);
  motor2.setSpeed(TURN_SPEED);
  motor3.setSpeed(TURN_SPEED);
  motor4.setSpeed(TURN_SPEED);
  
  motor1.run(BACKWARD);
  motor2.run(BACKWARD);
  motor3.run(FORWARD);
  motor4.run(FORWARD);
  
  delay(400);  // 转弯时间，根据实际情况调整
  
  moveStop();
  goesForward = true;
}
```



------

## 🔧 机械组装步骤

### 步骤1：安装电机

1. 将4个直流电机固定在小车底盘上
2. 确保左右两侧电机方向一致
3. 安装轮子并拧紧

### 步骤2：安装Arduino和驱动模块

1. 将Arduino UNO固定在小车中间位置
2. 将电机驱动模块固定在Arduino旁边
3. 使用尼龙柱或双面胶固定

### 步骤3：安装传感器

1. 将舵机安装在小车前方
2. 使用热熔胶将超声波传感器固定在舵机舵盘上
3. 确保传感器可以左右旋转90度

### 步骤4：电池安装

1. 将锂电池固定在小车后部
2. 使用扎带固定，确保不会松动

------

## ⚙️ 代码调试与校准

### 超声波传感器校准

上传以下测试代码检查传感器：

cpp

```
void setup() {
  Serial.begin(9600);
  pinMode(TRIG_PIN, OUTPUT);
  pinMode(ECHO_PIN, INPUT);
}

void loop() {
  digitalWrite(TRIG_PIN, LOW);
  delayMicroseconds(2);
  digitalWrite(TRIG_PIN, HIGH);
  delayMicroseconds(10);
  digitalWrite(TRIG_PIN, LOW);
  
  long duration = pulseIn(ECHO_PIN, HIGH);
  int distance = duration * 0.034 / 2;
  
  Serial.print("距离: ");
  Serial.print(distance);
  Serial.println(" cm");
  delay(500);
}
```



### 电机方向测试

如果小车前进时倒退，或左右转方向不对，可以调整电机接线：

- 交换任意一个电机的两根线即可改变方向

### 舵机角度校准

1. 确保舵机初始位置为90度（正前方）
2. 可以调整`lookRight()`和`lookLeft()`函数中的角度值

------

## 🚗 功能测试流程

### 测试1：基本功能

1. 上传完整代码
2. 打开串口监视器（波特率9600）
3. 观察串口输出信息
4. 将小车放在地上，观察是否能正常启动

### 测试2：避障测试

1. 在小车前放置障碍物（距离20-30cm）
2. 观察小车是否：
   - 检测到障碍物
   - 停止并后退
   - 左右查看
   - 选择正确方向转弯

### 测试3：参数调整

如果避障效果不理想，可以调整以下参数：

cpp

```
#define OBSTACLE_DISTANCE 20  // 减小此值让小车更接近障碍物
#define SAFE_DISTANCE 30      // 安全距离阈值
#define TURN_SPEED 170        // 转向速度
```



------

## 🛠️ 故障排除

### 常见问题1：电机不转

- ✅ 检查电池电量
- ✅ 检查电机接线是否松动
- ✅ 检查驱动模块使能引脚

### 常见问题2：传感器读数不准确

- ✅ 检查Trig和Echo线是否接反
- ✅ 确保传感器前方没有遮挡
- ✅ 尝试降低读取频率

### 常见问题3：小车乱转

- ✅ 检查地面是否平整
- ✅ 调整左右电机速度平衡
- ✅ 校准超声波传感器

### 常见问题4：舵机抖动

- ✅ 检查电源电压是否足够
- ✅ 确保舵机机械结构没有卡住
- ✅ 尝试降低舵机速度

------

## 📈 优化建议

### 性能优化：

1. **添加LED指示灯**：显示小车状态
2. **增加蓝牙模块**：手机遥控功能
3. **添加红外传感器**：检测低矮障碍物
4. **使用PID控制**：更平滑的转向

### 安全优化：

1. **添加急停开关**
2. **增加电压检测**：防止电池过放
3. **设置安全超时**：防止程序卡死

------

## 📚 学习资源

### 推荐进一步学习：

1. **Arduino官方教程**：[www.arduino.cc](https://www.arduino.cc/)
2. **电子元件基础**：学习电阻、电容、二极管等
3. **C++编程基础**：深入学习Arduino编程
4. **机器人学基础**：了解PID控制、路径规划

### 扩展项目：

1. 巡线小车
2. 蓝牙遥控小车
3. 手机APP控制小车
4. 添加摄像头实现图像识别

------

## ⚠️ 安全注意事项

1. **电池安全**：
   - 不要短路锂电池
   - 充电时不要离开
   - 使用合适的充电器
2. **操作安全**：
   - 不要在小车运行时触摸运动部件
   - 避免在潮湿环境使用
   - 远离火源和高温
3. **维护安全**：
   - 断电后再进行接线调整
   - 定期检查线路是否磨损
   - 清洁小车时避免进水

------

## 🎯 成功标准

完成所有步骤后，你的小车应该：

- ✅ 上电后自动启动
- ✅ 平稳直线前进
- ✅ 检测到障碍物自动停止
- ✅ 智能选择避障方向
- ✅ 成功避开障碍物继续前进

------

## 💡 温馨提示

1. **耐心调试**：第一次可能不会完美，多调试几次
2. **分步测试**：先测试各个模块，再组合测试
3. **记录问题**：遇到问题时记录下来，方便解决
4. **加入社区**：加入Arduino爱好者社区，交流学习

**祝你制作成功！如果有任何问题，随时可以提问。** 🚗✨

------

*本教程适用于初学者，建议在有经验的人员指导下完成。制作过程中请注意安全，遵守电子设备操作规范。*



### 



