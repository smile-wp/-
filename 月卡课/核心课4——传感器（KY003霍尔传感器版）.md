# 核心课4——传感器（KY003霍尔传感器版）

## 霍尔传感器及应用

### 🎯 今日学习目标

1. 了解霍尔效应原理
2. 学会使用KY003霍尔传感器检测磁场
3. 掌握KY003传感器的工作方式和特点
4. 实现KY003霍尔传感器控制LED、舵机、音乐播放器
5. 完成综合项目：磁控音乐盒（KY003版）

### ⏰ 课程时间分配（90分钟）

#### **第一部分：课程导入（10分钟）**

- **霍尔传感器应用展示（5分钟）**：手机翻盖检测、电动车测速、门窗报警
- **KY003传感器介绍（5分钟）**：
  - "KY003霍尔传感器模块特点：数字输出，灵敏度可调"
  - "为什么选择KY003：集成度高，使用简单，价格便宜"
  - "应用场景：磁性开关、转速测量、位置检测"

#### **第二部分：理论知识讲解（20分钟）**

**1. 霍尔效应原理（10分钟）**

**2. KY003霍尔传感器特性（5分钟）**

text

```
KY003霍尔传感器模块：
├── 传感器类型：数字型（开关型）
├── 输出信号：数字信号（0/1）
├── 特点：
│   ├── 工作电压：3.3V-5V
│   ├── 输出电流：15mA
│   ├── 灵敏度可调
│   ├── 带LED指示灯
│   └── 可直接驱动继电器、LED等
├── 引脚定义：
│   ├── VCC：电源正极（3.3-5V）
│   ├── GND：电源负极
│   └── OUT：信号输出（有磁场时输出低电平）
└── 应用场景：
    ├── 磁性开关
    ├── 速度检测
    ├── 位置检测
    └── 计数应用
```



**3. KY003工作原理（5分钟）**

- 当磁场强度超过阈值时，输出低电平
- 内置比较器，灵敏度可通过电位器调节
- 模块自带指示灯，直观显示状态

#### **第三部分：动手实践（40分钟）**

**实践1：KY003基础使用（10分钟）**

**电路连接：**

text

```
KY003霍尔传感器模块：
  VCC → 5V
  GND → GND
  OUT → 引脚2（支持中断）

LED指示灯：引脚13（用于视觉反馈）
```



**代码实现：**

cpp

```
/*
 * 项目：KY003霍尔传感器测试
 * 功能：检测磁铁靠近，统计次数
 * 注意：KY003有磁场时输出LOW，无磁场时输出HIGH
 */

// 引脚定义
int hallPin = 2;      // KY003输出引脚
int ledPin = 13;      // 状态指示灯

// 变量
volatile int magnetCount = 0;  // 磁铁检测次数
unsigned long lastDisplay = 0;

void setup() {
  pinMode(hallPin, INPUT_PULLUP);  // 启用内部上拉电阻
  pinMode(ledPin, OUTPUT);
  
  // 设置中断：引脚2状态变化时触发中断函数
  // FALLING：下降沿触发（磁铁靠近，输出从HIGH变LOW）
  attachInterrupt(digitalPinToInterrupt(hallPin), magnetDetected, FALLING);
  
  Serial.begin(9600);
  Serial.println("KY003霍尔传感器测试");
  Serial.println("请用磁铁靠近传感器");
  Serial.println("LED亮表示检测到磁铁");
  Serial.println("=================================");
}

void loop() {
  // 每2秒显示一次计数
  if (millis() - lastDisplay > 2000) {
    Serial.print("磁铁检测次数: ");
    Serial.println(magnetCount);
    lastDisplay = millis();
  }
  
  // 实时显示传感器状态
  static unsigned long lastStatusTime = 0;
  if (millis() - lastStatusTime > 500) {
    int sensorState = digitalRead(hallPin);
    Serial.print("传感器状态: ");
    Serial.println(sensorState == LOW ? "检测到磁场" : "无磁场");
    lastStatusTime = millis();
  }
}

// 中断服务函数
void magnetDetected() {
  magnetCount++;
  
  // LED快速闪烁表示检测到磁铁
  digitalWrite(ledPin, HIGH);
  delayMicroseconds(20000);  // 20ms LED亮，视觉反馈
  digitalWrite(ledPin, LOW);
  
  Serial.print("检测到磁铁！总次数: ");
  Serial.println(magnetCount);
}
```



**实践2：KY003控制舵机（15分钟）**

**电路连接：**

text

```
KY003霍尔传感器模块：
  VCC → 5V
  GND → GND
  OUT → 引脚2

舵机：
  信号线（橙色/黄色） → 引脚9
  电源线（红色） → 5V
  地线（棕色/黑色） → GND
```



**代码实现：**

cpp

```
/*
 * 项目：磁控舵机门锁（KY003版）
 * 功能：用磁铁控制舵机模拟门锁开关
 */

#include <Servo.h>

Servo doorLock;      // 舵机模拟门锁
int servoPin = 9;
int hallPin = 2;     // KY003霍尔传感器

// 门锁状态
bool doorLocked = true;
bool lastMagnetState = false;
unsigned long lastActionTime = 0;
const unsigned long debounceTime = 500;  // 防抖时间500ms

void setup() {
  doorLock.attach(servoPin);
  pinMode(hallPin, INPUT_PULLUP);  // 使用内部上拉
  
  Serial.begin(9600);
  Serial.println("磁控舵机门锁系统（KY003版）");
  Serial.println("====================");
  Serial.println("初始状态：门已锁定");
  Serial.println("用磁铁钥匙开锁/上锁");
  Serial.println("提示：磁铁靠近时解锁，移开时上锁");
  
  // 初始位置：锁定状态（0度）
  doorLock.write(0);
  delay(1000);
}

void loop() {
  // 读取KY003传感器状态（低电平表示磁铁靠近）
  bool magnetDetected = (digitalRead(hallPin) == LOW);
  
  // 检测状态变化
  if (magnetDetected != lastMagnetState) {
    lastActionTime = millis();  // 记录状态变化时间
  }
  
  // 防抖处理：状态稳定一段时间后才执行动作
  if ((millis() - lastActionTime > debounceTime) && 
      (magnetDetected != doorLocked)) {
    
    // 切换门锁状态
    doorLocked = !doorLocked;
    
    if (doorLocked) {
      lockDoor();
    } else {
      unlockDoor();
    }
    
    lastActionTime = millis();  // 防止重复触发
  }
  
  lastMagnetState = magnetDetected;
  
  // 状态显示
  static unsigned long lastStatusTime = 0;
  if (millis() - lastStatusTime > 1000) {
    Serial.print("状态: ");
    Serial.print(doorLocked ? "已锁定" : "未锁定");
    Serial.print(" | 传感器: ");
    Serial.println(magnetDetected ? "检测到磁铁" : "无磁铁");
    lastStatusTime = millis();
  }
  
  delay(50);
}

void unlockDoor() {
  Serial.println(">>> 正在开锁...");
  
  // 平滑开锁动作（0度 → 90度）
  for (int angle = 0; angle <= 90; angle += 2) {
    doorLock.write(angle);
    delay(30);
  }
  
  Serial.println(">>> 门已解锁");
}

void lockDoor() {
  Serial.println(">>> 正在上锁...");
  
  // 平滑上锁动作（90度 → 0度）
  for (int angle = 90; angle >= 0; angle -= 2) {
    doorLock.write(angle);
    delay(30);
  }
  
  Serial.println(">>> 门已锁定");
}
```



**实践3：KY003控制音乐播放（15分钟）**

**电路连接：**

text

```
KY003霍尔传感器模块：
  VCC → 5V
  GND → GND
  OUT → 引脚2

有源蜂鸣器：
  正极 → 引脚8
  负极 → GND
```



**代码实现：**

cpp

```
/*
 * 项目：磁控音乐播放器（KY003版）
 * 功能：用磁铁控制音乐的播放和停止
 * 说明：使用蜂鸣器播放简单音调
 */

int hallPin = 2;     // KY003霍尔传感器
int buzzerPin = 8;   // 蜂鸣器引脚

// 音乐定义
int melody[] = {262, 294, 330, 349, 392, 440, 494, 523};  // C大调音阶
int noteDuration = 200;  // 音符时长

// 播放状态
bool isPlaying = false;
int currentNote = 0;
unsigned long lastNoteTime = 0;

void setup() {
  pinMode(hallPin, INPUT_PULLUP);
  pinMode(buzzerPin, OUTPUT);
  
  Serial.begin(9600);
  Serial.println("磁控音乐播放器（KY003版）");
  Serial.println("========================");
  Serial.println("功能：磁铁靠近播放音乐，移开停止");
  Serial.println("音乐：C大调音阶循环播放");
}

void loop() {
  // 读取传感器状态
  bool magnetDetected = (digitalRead(hallPin) == LOW);
  
  if (magnetDetected && !isPlaying) {
    // 开始播放音乐
    isPlaying = true;
    currentNote = 0;
    lastNoteTime = millis();
    Serial.println(">>> 开始播放音乐");
    playNote(melody[currentNote]);
  } 
  else if (!magnetDetected && isPlaying) {
    // 停止播放
    isPlaying = false;
    noTone(buzzerPin);
    Serial.println(">>> 停止播放音乐");
  }
  
  // 如果正在播放，按时间切换音符
  if (isPlaying && (millis() - lastNoteTime > noteDuration)) {
    currentNote = (currentNote + 1) % 8;  // 循环8个音符
    playNote(melody[currentNote]);
    lastNoteTime = millis();
    
    // 显示当前播放的音符
    Serial.print("播放音符: ");
    Serial.println(currentNote + 1);
  }
  
  delay(10);
}

void playNote(int frequency) {
  tone(buzzerPin, frequency, noteDuration);
}
```



#### **第四部分：知识总结与扩展（15分钟）**

**知识总结（5分钟）**

- 霍尔效应原理
- KY003霍尔传感器的特点和使用方法
- 数字型霍尔传感器的工作方式
- 防抖处理技巧
- DFPlayer Mini MP3模块的基本使用方法

**扩展活动：综合项目 - 霍尔开关控制MP3播放器（10分钟）**

**项目介绍：**
制作一个磁控音乐播放器，使用KY003霍尔传感器控制DFPlayer Mini MP3模块。当磁铁靠近霍尔传感器时，系统会自动播放下一首歌曲，实现非接触式音乐控制。

**所需材料：**

1. KY003霍尔传感器模块
2. DFPlayer Mini MP3模块
3. Arduino Uno/Nano开发板
4. 扬声器（3W-5W）
5. Micro SD卡（包含MP3音乐文件）
6. 连接线若干

**电路连接：**

text

```
KY003霍尔传感器模块：
  VCC → Arduino 5V
  GND → Arduino GND
  OUT → Arduino 引脚2

DFPlayer Mini MP3模块：
  VCC → Arduino 5V
  GND → Arduino GND
  RX  → Arduino 引脚10
  TX  → Arduino 引脚11
  SPK1/SPK2 → 扬声器
```



**SD卡准备：**

1. 将MP3音乐文件重命名为0001.mp3、0002.mp3、0003.mp3...0010.mp3
2. 将这些文件放入SD卡根目录
3. 确保总歌曲数量与代码中的`totalTracks`变量一致

**代码实现：**

cpp

```
#include "Arduino.h"
#include "SoftwareSerial.h"
#include "DFRobotDFPlayerMini.h"

// 设置软件串口
SoftwareSerial mySoftwareSerial(10, 11); // RX, TX
DFRobotDFPlayerMini myDFPlayer;

// 霍尔传感器引脚
const int hallSensorPin = 2;

// 变量用于检测状态变化
int lastHallState = HIGH;
int currentHallState;
unsigned long lastDebounceTime = 0;
unsigned long debounceDelay = 50; // 防抖延迟(毫秒)

// 当前播放的歌曲编号
int currentTrack = 1;
// 总歌曲数量（根据您的SD卡中的实际歌曲数量修改）
const int totalTracks = 10;

void setup() {
  // 初始化硬件串口
  Serial.begin(115200);
  
  // 初始化软件串口
  mySoftwareSerial.begin(9600);
  
  // 设置霍尔传感器引脚为输入模式
  pinMode(hallSensorPin, INPUT_PULLUP);
  
  Serial.println();
  Serial.println(F("初始化DFPlayer ..."));
  
  // 延迟一段时间确保模块启动
  delay(2000);
  
  // 尝试初始化DFPlayer
  if (!myDFPlayer.begin(mySoftwareSerial)) {
    Serial.println(F("无法初始化DFPlayer:"));
    Serial.println(F("1. 请检查接线是否正确"));
    Serial.println(F("2. 请检查SD卡是否插入"));
    while(true);
  }
  
  Serial.println(F("DFPlayer Mini 初始化成功!"));
  
  // 设置初始参数
  myDFPlayer.volume(15);  // 设置音量(0~30)
  myDFPlayer.EQ(0);       // 设置均衡器
  
  Serial.println(F("系统就绪，等待磁铁靠近霍尔传感器..."));
  Serial.println(F("每次磁铁靠近将播放下一首歌曲"));
}

void loop() {
  // 读取霍尔传感器状态
  int reading = digitalRead(hallSensorPin);
  
  // 检查是否状态变化（防抖处理）
  if (reading != lastHallState) {
    lastDebounceTime = millis();
  }
  
  if ((millis() - lastDebounceTime) > debounceDelay) {
    // 状态稳定后的处理
    if (reading != currentHallState) {
      currentHallState = reading;
      
      // 检测到磁铁靠近（低电平触发）
      if (currentHallState == LOW) {
        playNextTrack();
      }
    }
  }
  
  lastHallState = reading;
  
  // 处理DFPlayer反馈（如果有）
  if (myDFPlayer.available()) {
    printDetail(myDFPlayer.readType(), myDFPlayer.read());
  }
}

// 播放下一首歌曲
void playNextTrack() {
  currentTrack++;
  if (currentTrack > totalTracks) {
    currentTrack = 1; // 循环回到第一首
  }
  
  Serial.print(F("播放第 "));
  Serial.print(currentTrack);
  Serial.println(F(" 首歌曲"));
  
  myDFPlayer.play(currentTrack);
}

// 打印DFPlayer的详细状态信息
void printDetail(uint8_t type, int value){
  switch (type) {
    case TimeOut:
      Serial.println(F("操作超时!"));
      break;
    case WrongStack:
      Serial.println(F("堆栈错误!"));
      break;
    case DFPlayerCardInserted:
      Serial.println(F("SD卡插入!"));
      break;
    case DFPlayerCardRemoved:
      Serial.println(F("SD卡拔出!"));
      break;
    case DFPlayerCardOnline:
      Serial.println(F("SD卡就绪!"));
      break;
    case DFPlayerPlayFinished:
      Serial.print(F("播放完成:"));
      Serial.println(value);
      break;
    case DFPlayerError:
      Serial.print(F("错误:"));
      switch (value) {
        case Busy:
          Serial.println(F("卡忙"));
          break;
        case Sleeping:
          Serial.println(F("休眠中"));
          break;
        case SerialWrongStack:
          Serial.println(F("串口堆栈错误"));
          break;
        case CheckSumNotMatch:
          Serial.println(F("校验和不匹配"));
          break;
        case FileIndexOut:
          Serial.println(F("文件索引超出范围"));
          break;
        case FileMismatch:
          Serial.println(F("找不到文件"));
          break;
        case Advertise:
          Serial.println(F("广告模式"));
          break;
        default:
          break;
      }
      break;
    default:
      break;
  }
}
```



**项目功能说明：**

1. 系统启动后自动初始化DFPlayer Mini模块
2. 每次磁铁靠近KY003霍尔传感器，系统会自动播放下一首歌曲
3. 播放到最后一首歌曲后，会自动循环到第一首
4. 串口监视器会显示系统状态和播放信息
5. 代码包含完善的错误处理和状态反馈

**扩展挑战：**

1. 修改代码，实现长按磁铁暂停/播放功能
2. 添加LED指示灯，显示播放状态
3. 实现双击磁铁切换播放模式（顺序播放/随机播放）
4. 添加音量控制功能（通过多次快速靠近磁铁调节音量）

**家庭作业：**

1. 设计一个磁铁接近报警器，当磁铁靠近时蜂鸣器报警
2. 制作一个磁铁计数器，统计磁铁靠近次数并显示在串口监视器
3. 研究如何用KY003传感器测量电机转速（提示：统计单位时间内磁铁经过的次数）
4. 扩展项目：制作一个磁控门铃，用磁铁作为门铃按钮

------

**修改说明：**

1. 将所有霍尔传感器相关内容统一改为使用KY003模块
2. 移除了模拟霍尔传感器相关内容（49E、模拟量读取等）
3. 调整了代码示例，全部适配KY003的数字输出特性
4. 更新了第四部分的扩展项目，使用KY003控制DFPlayer Mini MP3模块
5. 更新了电路连接图描述，明确KY003和DFPlayer Mini的接线方式
6. 调整了教学目标，专注于KY003数字霍尔传感器的应用